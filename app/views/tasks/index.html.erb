<div class="container">
  <% @tasks.each do |t| %>
    <p><span><%= t.completed == false ? "[ ]" : "[x]"  %></span> <%= link_to t.title, task_path(t) %></p>
    <p><%= link_to "edit", edit_path(t)  %></p>
  <% end %>
  <p><%= link_to "Add a new task", new_path %></p>
</div>
